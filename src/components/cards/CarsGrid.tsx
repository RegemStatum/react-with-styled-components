import React, { FC } from "react";
import styled from "styled-components";

interface CarsGridProps {}

const CarsGrid: FC<CarsGridProps> = () => {
  return <Wrapper>CarsGrid</Wrapper>;
};

const Wrapper = styled.div``;

export default CarsGrid;
