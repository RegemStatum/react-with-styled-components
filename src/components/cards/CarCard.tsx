import React, { FC } from "react";
import styled from "styled-components";

interface CarCardProps {}

const CarCard: FC<CarCardProps> = () => {
  return <Wrapper>CarCard</Wrapper>;
};

const Wrapper = styled.div``;

export default CarCard;
