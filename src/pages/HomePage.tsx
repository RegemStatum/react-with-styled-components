import React, { FC } from "react";
import styled from "styled-components";

const HomePage: FC = () => {
  return <Wrapper className="container">HomePage</Wrapper>;
};

const Wrapper = styled.div``;

export default HomePage;
