import React, { FC } from "react";
import styled from "styled-components";

const AboutPage: FC = () => {
  return <Wrapper>AboutPage</Wrapper>;
};

const Wrapper = styled.div``;

export default AboutPage;
